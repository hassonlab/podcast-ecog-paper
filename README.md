# Monkey in the middle dataset paper

To reproduce figures in the paper, run the following commands:

1. `make erp`: run the ERP QA analysis
2. `make audioxcorr`: run the audio-ecog cross-correlation QA analysis
3. `make encoding`: run separate encoding models on five different feature spaces